---
---

<section class="process-section">
  <div class="container">
    <div class="process-header">
      <h3>¿Cómo funciona Container Connect?</h3>
      <p>Proceso simple y transparente en 4 pasos</p>
    </div>
    
    <div class="process-steps">
      <div class="step">
        <div class="step-number">1</div>
        <div class="step-icon">📱</div>
        <div class="step-content">
          <h4>Regístrate en 2 minutos</h4>
          <p>Completa el formulario con tus datos y necesidades específicas. Sin compromiso, acceso prioritario garantizado.</p>
        </div>
        <div class="step-arrow">→</div>
      </div>
      
      <div class="step">
        <div class="step-number">2</div>
        <div class="step-icon">🔍</div>
        <div class="step-content">
          <h4>Elige tu contenedor ideal</h4>
          <p>Selecciona tamaño, ubicación y fechas. Precios transparentes desde 65€/semana con todo incluido.</p>
        </div>
        <div class="step-arrow">→</div>
      </div>
      
      <div class="step">
        <div class="step-number">3</div>
        <div class="step-icon">🚚</div>
        <div class="step-content">
          <h4>Recíbelo en 24 horas</h4>
          <p>Entrega garantizada por nuestros proveedores certificados. Seguimiento en tiempo real incluido.</p>
        </div>
        <div class="step-arrow">→</div>
      </div>
      
      <div class="step">
        <div class="step-number">4</div>
        <div class="step-icon">♻️</div>
        <div class="step-content">
          <h4>Gestionamos la recogida</h4>
          <p>Nos encargamos de la retirada y gestión de residuos según normativa. Soporte técnico 24/7.</p>
        </div>
      </div>
    </div>
    
    <div class="process-benefits">
      <div class="benefit">
        <span class="benefit-icon">⚡</span>
        <span class="benefit-text">Proceso 100% online</span>
      </div>
      <div class="benefit">
        <span class="benefit-icon">🛡️</span>
        <span class="benefit-text">Sin intermediarios</span>
      </div>
      <div class="benefit">
        <span class="benefit-icon">💰</span>
        <span class="benefit-text">Precio garantizado</span>
      </div>
      <div class="benefit">
        <span class="benefit-icon">📞</span>
        <span class="benefit-text">Soporte técnico incluido</span>
      </div>
    </div>
  </div>
</section>

<style>
  .process-section {
    padding: 5rem 0;
    background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
    position: relative;
    overflow: hidden;
  }
  
  .process-section::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(46, 134, 171, 0.05) 0%, transparent 70%);
    animation: float-reverse 25s ease-in-out infinite;
  }
  
  @keyframes float-reverse {
    0%, 100% { transform: translate(0, 0) rotate(0deg); }
    50% { transform: translate(30px, -30px) rotate(-1deg); }
  }
  
  .process-header {
    text-align: center;
    margin-bottom: 4rem;
    position: relative;
    z-index: 1;
  }
  
  .process-header h3 {
    font-size: 2.8rem;
    color: var(--neutral-dark);
    margin-bottom: 1rem;
    font-weight: 700;
  }
  
  .process-header p {
    font-size: 1.3rem;
    color: var(--neutral-medium);
    opacity: 0.8;
  }
  
  .process-steps {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
    position: relative;
    z-index: 1;
  }
  
  .step {
    position: relative;
    background: white;
    padding: 2rem;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
    border: 2px solid transparent;
    transition: all 0.4s ease;
    text-align: center;
  }
  
  .step:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    border-color: var(--primary-color);
  }
  
  .step-number {
    position: absolute;
    top: -15px;
    left: 50%;
    transform: translateX(-50%);
    width: 30px;
    height: 30px;
    background: linear-gradient(135deg, var(--primary-color), #ff8c42);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 0.9rem;
    box-shadow: 0 5px 15px rgba(255, 107, 53, 0.4);
  }
  
  .step-icon {
    font-size: 3rem;
    margin: 1rem 0;
    display: block;
  }
  
  .step-content h4 {
    font-size: 1.3rem;
    color: var(--neutral-dark);
    margin-bottom: 1rem;
    font-weight: 600;
  }
  
  .step-content p {
    color: var(--neutral-medium);
    line-height: 1.6;
    font-size: 0.95rem;
  }
  
  .step-arrow {
    display: none;
    position: absolute;
    right: -30px;
    top: 50%;
    transform: translateY(-50%);
    font-size: 2rem;
    color: var(--primary-color);
    font-weight: bold;
  }
  
  .process-benefits {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 2rem;
    margin-top: 3rem;
    position: relative;
    z-index: 1;
  }
  
  .benefit {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    background: white;
    padding: 1rem 1.5rem;
    border-radius: 50px;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
    border: 1px solid rgba(255, 107, 53, 0.2);
    transition: all 0.3s ease;
  }
  
  .benefit:hover {
    transform: scale(1.05);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.12);
    border-color: var(--primary-color);
  }
  
  .benefit-icon {
    font-size: 1.2rem;
  }
  
  .benefit-text {
    font-weight: 600;
    color: var(--neutral-dark);
    font-size: 0.9rem;
  }
  
  /* Desktop layout with arrows */
  @media (min-width: 1024px) {
    .process-steps {
      grid-template-columns: repeat(4, 1fr);
      gap: 0;
    }
    
    .step {
      margin: 0 1rem;
    }
    
    .step:not(:last-child) .step-arrow {
      display: block;
    }
  }
  
  @media (max-width: 768px) {
    .process-header h3 {
      font-size: 2.2rem;
    }
    
    .process-header p {
      font-size: 1.1rem;
    }
    
    .process-steps {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    
    .step {
      padding: 1.5rem;
    }
    
    .step-icon {
      font-size: 2.5rem;
    }
    
    .step-content h4 {
      font-size: 1.2rem;
    }
    
    .process-benefits {
      flex-direction: column;
      align-items: center;
      gap: 1rem;
    }
    
    .benefit {
      padding: 0.8rem 1.2rem;
    }
  }
</style>