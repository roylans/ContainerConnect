---
---

<section class="section benefits">
  <div class="container">
    <h2 class="section-title">¿Por qué elegir nuestra plataforma?</h2>
    <div class="benefits-grid">
      <div class="benefit-card">
        <div class="benefit-icon">
          💰
        </div>
        <h3>Mejor Precio</h3>
        <p>Comparamos automáticamente precios de múltiples proveedores</p>
        <div class="benefit-highlight">
          Ahorra hasta un 25% vs contactar directo
        </div>
      </div>

      <div class="benefit-card">
        <div class="benefit-icon">
          ⏰
        </div>
        <h3>Disponibilidad Real</h3>
        <p>Ve disponibilidad en tiempo real antes de solicitar</p>
        <div class="benefit-highlight">
          Sin esperas ni sorpresas de última hora
        </div>
      </div>

      <div class="benefit-card">
        <div class="benefit-icon">
          🚛
        </div>
        <h3>Servicio Nacional</h3>
        <p>Proveedores verificados en toda España con servicio local</p>
        <div class="benefit-highlight">
          Red nacional con proveedores locales en cada ciudad
        </div>
      </div>
    </div>

    <div class="additional-benefits">
      <div class="benefit-row">
        <div class="benefit-item">
          <span class="check">✅</span>
          <span>Gestión 100% online</span>
        </div>
        <div class="benefit-item">
          <span class="check">✅</span>
          <span>Soporte técnico especializado</span>
        </div>
        <div class="benefit-item">
          <span class="check">✅</span>
          <span>Facturación simplificada</span>
        </div>
        <div class="benefit-item">
          <span class="check">✅</span>
          <span>Seguimiento de entregas en tiempo real</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .benefits {
    background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
  }

  .section-title {
    font-size: 2.5rem;
    text-align: center;
    margin-bottom: 3rem;
    color: var(--neutral-dark);
  }

  .benefits-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .benefit-card {
    background: white;
    padding: 2.5rem 2rem;
    border-radius: 20px;
    text-align: center;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .benefit-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
  }

  .benefit-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
  }

  .benefit-icon {
    font-size: 4rem;
    margin-bottom: 1rem;
    display: block;
  }

  .benefit-card h3 {
    font-size: 1.5rem;
    color: var(--secondary-color);
    margin-bottom: 1rem;
  }

  .benefit-card p {
    font-size: 1rem;
    color: var(--neutral-medium);
    margin-bottom: 1.5rem;
    line-height: 1.6;
  }

  .benefit-highlight {
    background: linear-gradient(135deg, var(--primary-color), #ff8c42);
    color: white;
    padding: 0.75rem 1rem;
    border-radius: 25px;
    font-weight: 600;
    font-size: 0.9rem;
  }

  .additional-benefits {
    background: white;
    padding: 2rem;
    border-radius: 15px;
    box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
  }

  .benefit-row {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
  }

  .benefit-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    font-size: 1rem;
    font-weight: 500;
  }

  .check {
    color: var(--success-color);
    font-size: 1.2rem;
  }

  @media (max-width: 768px) {
    .section-title {
      font-size: 2rem;
    }

    .benefits-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .benefit-card {
      padding: 2rem 1.5rem;
    }

    .benefit-icon {
      font-size: 3rem;
    }

    .benefit-row {
      grid-template-columns: 1fr;
      gap: 1rem;
    }
  }
</style>